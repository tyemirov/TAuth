services:
  tauth:
    image: ghcr.io/tyemirov/tauth:latest
    env_file:
      - ./.env.tauth
    ports:
      - "8080:8080"
    volumes:
      - tauth_data:/data
    restart: unless-stopped

volumes:
  tauth_data:
